# Active Workstreams

One row per stream. Keep this short. Move finished streams to the
`Recently completed` section, then delete after a release.

## Active

| Stream | Owner | Status | Where | Notes |
| --- | --- | --- | --- | --- |
| Multi-scanner backend (`scanner_profiles/`) | Other contributor (committed via `disturbedkh` upstream) | Foundation landed in `v0.9.0b2`. `ACTIVE_PROFILE` is wired but never reassigned at runtime. Migration of remaining module-level constants is pending. | `scanner_profiles/`, `scanner_manager.py`, `metastore.py`, `tests/test_scanner_profiles.py`, `tests/test_bt885_parity.py` | See `MULTI_SCANNER_BACKEND.md` for the full plan and don't-touch list. |
| **SDS100 profile (and SDS200 by extension)** | Local | SD-card RE complete (see `RE/SDS100.md` + `RE/SD_CARD_COMPARISON.md`); live serial-mode RE in progress (Session 2 captured the MAIN-processor command port). Profile not yet implemented. | New `scanner_profiles/sds100.py` (planned), `data/scanner_profiles.json`, `tests/test_sds100_parity.py` (planned), plus changes to detection in `scanner_profiles/registry.py` or a new `detect_from_card()` helper. | Real card RE'd 2026-04-27 from `D:\` (FW 1.23.07). Live SDS100 Serial Mode RE same day: `MDL,SDS100`, full Remote Command Protocol on PID `0x001A` only. Tooling in `AI/Dev/RE/serial_probe.py` + `com6_listen.py`, captures in `AI/Dev/RE/sessions/`. SDS200 expected to share ~99% of code/data layout - one profile should cover both. |
| **BT885 SD card RE (verified)** | Local | Done 2026-04-27 on `<HOST>` from real BT885 card on `E:\`. Notes in `RE/BT885.md`; family diff in `RE/SD_CARD_COMPARISON.md`. Confirmed firmware tables are bit-identical to SDS100; `TargetModel` is `BCDx36HP` not `Beartracker885`. | `AI/Dev/RE/BT885.md`, `AI/Dev/RE/SD_CARD_COMPARISON.md`, `AI/Dev/RE/compare_cards.py`. | Invalidates `Bt885Profile.target_model_aliases` and several test fixtures. Drives the detect-on-open refactor below. |
| **SDS100 live-serial RE (passive read-only)** | Local | Sessions 1-6 done. Session 6 (2026-04-29, Tier ABC plan) added the SUB-port systematic probe (`sub_probe.py` alphabet-attack), MAIN-port arg-extension probes, and BCDx36HP V1.05 legacy probes. Net new: `U` (SUB - 9-byte register dump), `GSI,XML/RAW/PROP/FULL` (MAIN), `GLT,SYS` (MAIN, undocumented). Catalog: `AI/Dev/RE/SDS100_unofficial_commands.md`. **35 untriggered Sub printf format strings** identified as future probe targets. | `AI/Dev/RE/SDS100.md` (Sessions 1-6), `AI/Dev/RE/SDS100_unofficial_commands.md`, `AI/Dev/RE/serial_probe.py`, `AI/Dev/RE/sub_probe.py`, `AI/Dev/RE/sessions/` | Strict rules unchanged. **Phase 4 (Sentinel USB capture) pending user time** - workflow ready in `AI/Dev/RE/sentinel_capture.md`. |
| **SDS100 firmware static RE** | Local | **Sub payload extracted Session 6 (2026-04-29).** Container parser ([`_inflate_sub.py`](AI/Dev/RE/_inflate_sub.py)) corrected the prior "compressed" assumption: Sub firmware is plaintext ARM Cortex-M, not zlib/LZMA. Output: [`firmware/sub_1.03.15_inflated.bin`](AI/Dev/RE/firmware/sub_1.03.15_inflated.bin) (90,076 bytes). Reset vector decoded: SP=0x10020000, PC=0x140001D5 (Thumb in SPIFI flash). Best-effort static analysis ([`_sub_static_analysis.py`](AI/Dev/RE/_sub_static_analysis.py)) tagged UART1 + SSP0 as the most likely inter-MCU bus candidates (6 refs each). Main `.bin` remains encrypted/infeasible. | `AI/Dev/RE/SDS100_firmware.md` (Session 6 update), `AI/Dev/RE/ghidra_import_runbook.md`, `AI/Dev/RE/sub_command_response_map.md`, `AI/Dev/RE/sub_static_analysis.md`, `AI/Dev/RE/firmware/sub_1.03.15_inflated.bin`, `AI/Dev/RE/firmware/sub_1.03.15_chunk_map.md` | **Phase 6.2 (Ghidra import) is the unblocker** for full Sub disassembly - estimated 20-50h of focused work. Runbook ready. SDS150 shares the firmware family. |
| **In-app firmware updater** | Local (planning) | Design doc landed `AI/Dev/FIRMWARE_UPDATER.md`. Implementation not started. **Unblocked by Session 4 RE finding** that Sentinel firmware updates are pure SD-card file-drops (no proprietary USB protocol), and **further unblocked 2026-05-03** by RE of the Uniden update endpoints (`AI/Dev/RE/docs/uniden_update_endpoints.md`) - both Sentinel and BT885 Update Manager pull from plain FTP with hardcoded creds. Discovery layer is now FTP listing, not TWiki scraping. Phase 1 (firmware library + FTP discovery, no scanner contact) is independent and can ship first. | New: `data/firmware_manifest.json` (enrichment only), `firmware_cache/`, `scanner_manager/uniden_ftp.py` (new), `scanner_manager/firmware_updater.py`, GUI panel. Existing: `AI/Dev/FIRMWARE_UPDATER.md`, `AI/Dev/RE/docs/uniden_update_endpoints.md`. | Risk profile: same as Sentinel itself (power loss mid-flash, wrong family file). Mitigations: pre-flash backup snapshot (reuse existing backup manager), SHA-256 verify pre-copy + post-copy, `scanner.inf` model-match guard, clear-folder-of-prior-bin guard. |
| **Uniden update endpoint RE** | Local | **Done 2026-05-03.** Both Sentinel (`ftp.homepatrol.com/BCDx36HP/`) and BT885 Update Manager (`ftp.uniden.com/BT885/`) extracted via static analysis of installer .NET binaries; live FTP listings captured. Sentinel server holds 105+ firmware blobs across the BCDx36HP family + 161 weekly HPDB snapshots. BT885 server: HPDB only. Documented endpoints, credentials, FTP commands used (`NLST`/`SIZE`/`MDTM`/`RETR`), and the `.app` marker-file trick. | `AI/Dev/RE/docs/uniden_update_endpoints.md`, `wiki/RE-Update-Endpoints.md`, `AI/Dev/RE/sentinel_decompile/strings/` (raw artifacts). | Drives the firmware-updater redesign above; also surfaces self-update + HPDB sync as natural side-effects. |
| **Multi-device GUI (top selector)** | Local (planning) | Design doc landed `AI/Dev/MULTI_DEVICE_GUI.md`. Builds on `MULTI_SCANNER_BACKEND.md` foundation. Adds top-of-window Device selector that swaps `ACTIVE_PROFILE` + per-tab support gating + connection status + firmware version display. Implementation not started. | New: `data/devices.json` (Device manifest), top header frame in `scanner_manager.py`, new `ScannerProfile` flags (`supports_serial_mode`, `supports_waterfall`, `supports_favorites_lists`, `usb_vid_pid_serial`). Existing: `AI/Dev/MULTI_DEVICE_GUI.md`, `AI/Dev/MULTI_SCANNER_BACKEND.md`. | Phases 1-5 sketched in the design doc. Phase 1 (backend flag additions + reassignable `ACTIVE_PROFILE`) is the prerequisite. Phase 4 (detect-on-open) shares the work with the existing backlog item "Detect-on-open scanner profile reassignment". |

## Backlog (not started)

| Stream | Trigger | Notes |
| --- | --- | --- |
| Detect-on-open scanner profile reassignment | After multi-scanner backend foundation lands | Step 1 of `MULTI_SCANNER_BACKEND.md`. **Blocks SDS100 profile** because `TargetModel` alone is insufficient on BCDx36HP-family cards - both BT885 and SDS100 write `TargetModel BCDx36HP` (verified 2026-04-27 against real cards on `<DRIVE>:\` and `H:\`). Detector needs to read `BCDx36HP/scanner.inf`'s `Scanner` field 1 (`BT885-SCN` vs `SDS100`). |
| Update BT885 test fixtures and aliases | After detect-on-open lands | Replace `TargetModel\tBeartracker885` fixtures in `tests/test_sdcard.py`, `tests/test_metastore.py`, `tests/test_merge_and_zip.py` with `TargetModel\tBCDx36HP`. Drop `("Beartracker885", ...)` from `Bt885Profile.target_model_aliases`. Driven by RE confirmation in `AI/Dev/RE/BT885.md`. |
| Retire `scanner_profiles/compat.py` | After all module-level constants in `scanner_manager.py` are migrated | Step 2 of `MULTI_SCANNER_BACKEND.md`. |
| Favorites Lists editor (SDS100 / SDS200) | After SDS100 profile lands | Round-trips today via the ancillary path; no UI to edit yet. |
| Discovery session reader | After we have a populated `discvery.cfg` to RE | Schema unknown until populated. |

## Recently completed

| Stream | Released in | Notes |
| --- | --- | --- |
| `scanner_profiles` package + `Bt885Profile` extraction | `v0.9.0b2` (2026-04-24) | First slice of multi-scanner work. |
| Map-tile coverage heatmap | `v0.9.0b2` | `tkintermapview` integration with fallback. |
| Multiple virtual SD card profiles + snapshot history | `v0.9.0b2` | `metastore.GlobalMetaStore` profiles, `<profile_dir>/.snapshots/`. |
| Plain-English UI sweep + tone linter | `v0.9.0b2` | Style guide enforced. |
